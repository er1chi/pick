/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { Options, RequestResult } from '../.kubb/client'
import type { RepoEditPullRequestOptions, RepoEditPullRequestResponses } from '../types/RepoEditPullRequest'
import { client } from '../.kubb/client'

/**
 * @summary Update a pull request. If using deadline only the date will be taken into account, and time of day ignored.
 * {@link /repos/:owner/:repo/pulls/:index}
 */
export function repoEditPullRequest<ThrowOnError extends boolean = true>(options: Options<RepoEditPullRequestOptions, ThrowOnError>): Promise<RequestResult<RepoEditPullRequestResponses, ThrowOnError>> {
  const { client: request = client, ...config } = options

  return request({ method: 'PATCH', url: '/repos/{owner}/{repo}/pulls/{index}', security: [{ type: 'http', scheme: 'basic' }, { type: 'apiKey', name: 'Authorization', in: 'header' }, { type: 'apiKey', name: 'sudo', in: 'query' }, { type: 'apiKey', name: 'Sudo', in: 'header' }, { type: 'apiKey', name: 'X-FORGEJO-OTP', in: 'header' }], ...config }) as Promise<RequestResult<RepoEditPullRequestResponses, ThrowOnError>>
}
