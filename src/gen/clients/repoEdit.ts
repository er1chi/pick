/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { Options, RequestResult } from '../.kubb/client'
import type { RepoEditOptions, RepoEditResponses } from '../types/RepoEdit'
import { client } from '../.kubb/client'

/**
 * @summary Edit a repository's properties. Only fields that are set will be changed.
 * {@link /repos/:owner/:repo}
 */
export function repoEdit<ThrowOnError extends boolean = true>(options: Options<RepoEditOptions, ThrowOnError>): Promise<RequestResult<RepoEditResponses, ThrowOnError>> {
  const { client: request = client, ...config } = options

  return request({ method: 'PATCH', url: '/repos/{owner}/{repo}', security: [{ type: 'http', scheme: 'basic' }, { type: 'apiKey', name: 'Authorization', in: 'header' }, { type: 'apiKey', name: 'sudo', in: 'query' }, { type: 'apiKey', name: 'Sudo', in: 'header' }, { type: 'apiKey', name: 'X-FORGEJO-OTP', in: 'header' }], ...config }) as Promise<RequestResult<RepoEditResponses, ThrowOnError>>
}
