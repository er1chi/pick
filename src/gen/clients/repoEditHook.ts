/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { Options, RequestResult } from '../.kubb/client'
import type { RepoEditHookOptions, RepoEditHookResponses } from '../types/RepoEditHook'
import { client } from '../.kubb/client'

/**
 * @summary Edit a hook in a repository
 * {@link /repos/:owner/:repo/hooks/:id}
 */
export function repoEditHook<ThrowOnError extends boolean = true>(options: Options<RepoEditHookOptions, ThrowOnError>): Promise<RequestResult<RepoEditHookResponses, ThrowOnError>> {
  const { client: request = client, ...config } = options

  return request({ method: 'PATCH', url: '/repos/{owner}/{repo}/hooks/{id}', security: [{ type: 'http', scheme: 'basic' }, { type: 'apiKey', name: 'Authorization', in: 'header' }, { type: 'apiKey', name: 'sudo', in: 'query' }, { type: 'apiKey', name: 'Sudo', in: 'header' }, { type: 'apiKey', name: 'X-FORGEJO-OTP', in: 'header' }], ...config }) as Promise<RequestResult<RepoEditHookResponses, ThrowOnError>>
}
