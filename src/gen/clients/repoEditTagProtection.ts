/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { Options, RequestResult } from '../.kubb/client'
import type { RepoEditTagProtectionOptions, RepoEditTagProtectionResponses } from '../types/RepoEditTagProtection'
import { client } from '../.kubb/client'

/**
 * @summary Edit a tag protections for a repository. Only fields that are set will be changed
 * {@link /repos/:owner/:repo/tag_protections/:id}
 */
export function repoEditTagProtection<ThrowOnError extends boolean = true>(options: Options<RepoEditTagProtectionOptions, ThrowOnError>): Promise<RequestResult<RepoEditTagProtectionResponses, ThrowOnError>> {
  const { client: request = client, ...config } = options

  return request({ method: 'PATCH', url: '/repos/{owner}/{repo}/tag_protections/{id}', security: [{ type: 'http', scheme: 'basic' }, { type: 'apiKey', name: 'Authorization', in: 'header' }, { type: 'apiKey', name: 'sudo', in: 'query' }, { type: 'apiKey', name: 'Sudo', in: 'header' }, { type: 'apiKey', name: 'X-FORGEJO-OTP', in: 'header' }], ...config }) as Promise<RequestResult<RepoEditTagProtectionResponses, ThrowOnError>>
}
